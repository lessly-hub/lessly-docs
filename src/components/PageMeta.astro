---
/**
 * Page-header meta row.
 * Sequence (spec §Page-header pattern):
 *   1. Diátaxis eyebrow label — neutral, no color.
 *   2. h1 page title.
 *   3. Lead paragraph.
 *
 * NOTE: The maturity status (alpha/beta/stable) lives in MDX frontmatter
 * and the content schema (`src/content.config.ts`) as a hidden mark for
 * writers/reviewers. It is intentionally NOT rendered — surfacing
 * "alpha"/"beta" pills to customers undermines trust in the product.
 */
interface Props {
  title: string;
  description: string;
  diataxis: 'tutorial' | 'how-to' | 'explanation' | 'reference';
}

const { title, description, diataxis } = Astro.props;

const diataxisLabel: Record<Props['diataxis'], string> = {
  tutorial: 'Tutorial',
  'how-to': 'How-to',
  explanation: 'Explanation',
  reference: 'Reference',
};
---

<header class="mb-10">
  <div
    class="mb-3 text-xs font-medium uppercase tracking-[0.04em] text-[var(--color-text-tertiary)]"
  >
    {diataxisLabel[diataxis]}
  </div>
  <h1 class="mb-3 text-4xl font-extrabold leading-[1.1] tracking-tight text-[var(--color-text-primary)]">
    {title}
  </h1>
  <p class="text-lg text-[var(--color-text-secondary)] leading-[1.5]">{description}</p>
</header>
